#README#

This folder contains the configuration and code for creating the docker images used for this example.
You can build and push your own images to Docker Hub or just use my images as referenced in the app.yml file.
